# appgestionbeta
1.
para empaquetar hay que importar babel.numbers sino no anda code = pyinstaller --noconsole --onefile --hidden-import babel.numbers --add-data "ruta_a_archivo;carpeta_destino" main.py
2.solo usamos librerias nativas no hay que instalar nada 
