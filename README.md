# appgestionbeta

1.para empaquetar hay que importar babel.numbers sino no anda code = yinstaller --noconsole --onefile --hidden-import babel.numbers main.py
2.solo usamos librerias nativas no hay que instalar nada 
